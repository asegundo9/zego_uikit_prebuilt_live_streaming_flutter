name: zego_uikit_prebuilt_live_streaming_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  zego_uikit_prebuilt_live_streaming:
    path: ../

  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  file_picker: 8.1.7
  connectivity_plus: 5.0.2
  device_info_plus: 11.3.0
  package_info_plus: 8.1.2
  flutter_lints: 4.0.0
  path: any
